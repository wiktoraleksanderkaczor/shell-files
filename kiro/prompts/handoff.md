Review the full session context — every file read, every change made, every decision taken, every constraint discovered, every error encountered, and every piece of reasoning applied — then produce a single paragraph that is entirely self-contained and standalone, meaning it must inline all necessary information directly (file paths, file contents, code snippets, configuration values, diffs, error messages, decisions, constraints) rather than referencing them by name or saying "see above" or "as discussed", because the receiving agent will have zero access to this session, zero access to prior messages, and zero access to any files unless the content is reproduced verbatim inside the paragraph itself; the paragraph must capture the complete problem statement, the current state of the work, all relevant technical details and nuances, what has been completed, what remains, and any blockers or open questions, written so that a new agent can take over and continue without loss of information; output the paragraph as unrendered markdown wrapped in a fenced code block, on a single line with no line breaks inside the paragraph, using no apostrophes and no contractions.
